import { ServerImgDirective } from './server-img.directive';

describe('ServerImgDirective', () => {
  it('should create an instance', () => {
    const directive = new ServerImgDirective();
    expect(directive).toBeTruthy();
  });
});
