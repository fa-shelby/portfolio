import { ServerLinkDirective } from './server-link.directive';

describe('ServerLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new ServerLinkDirective();
    expect(directive).toBeTruthy();
  });
});
